// create a variable called name, alert hello + name
const name = "Leo";
// create a password variable, make it simple and fake
const password = "123123";
const userTry = prompt("Enter the password!");

if (password == userTry) {
  alert("Hello " + name);
}
else {
  console.log("Error!");
}

// alert("Hello " + name);